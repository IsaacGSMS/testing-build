machine:
    node:
      version: 6.3.0

dependencies:
  pre:
    - rm -rf ./node_modules
  override:
    - npm install
  cache_directories:
    - ~/.npm

test:
  override:
    - npm test

deployment:
    master:
        branch: master
